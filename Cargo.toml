[package]
name = "typenv"
version = "0.1.0"
edition = "2021"

[dependencies]
env_logger = "0.11.5"
clap = { version = "4.5.16", features = ["derive"] }
log = { version = "0.4.22", features = [
  "max_level_debug",
  "release_max_level_debug",
] }
